query WishlistPage($wishlistId: WishlistId!) {
  wishlist(id: $wishlistId) {
    ... on Wishlist {
      id
      title
      description
      logoUrl
      owner {
        id
        firstName
        lastName
      }
    }
    ... on NotFoundRejection {
      __typename
    }
  }
}
