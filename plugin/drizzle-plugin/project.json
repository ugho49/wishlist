{
  "name": "drizzle-plugin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "plugin/drizzle-plugin/src",
  "projectType": "library",
  "tags": ["plugin"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/plugin/drizzle-plugin",
        "main": "plugin/drizzle-plugin/src/index.ts",
        "tsConfig": "plugin/drizzle-plugin/tsconfig.lib.json",
        "assets": [
          "plugin/drizzle-plugin/*.md",
          {
            "input": "./plugin/drizzle-plugin/src",
            "glob": "**/!(*.ts)",
            "output": "./src"
          },
          {
            "input": "./plugin/drizzle-plugin/src",
            "glob": "**/*.d.ts",
            "output": "./src"
          },
          {
            "input": "./plugin/drizzle-plugin",
            "glob": "generators.json",
            "output": "."
          },
          {
            "input": "./plugin/drizzle-plugin",
            "glob": "executors.json",
            "output": "."
          }
        ]
      }
    },
    "typecheck": {
      "cache": true,
      "inputs": ["production", "^production", { "externalDependencies": ["typescript"] }],
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.lib.json",
        "cwd": "{projectRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mkdir -p ../../junit_reports/unit && bun test $(find . -type f -name '*.spec.ts') --pass-with-no-tests --reporter=junit --reporter-outfile=../../junit_reports/unit/drizzle-plugin.xml",
        "cwd": "{projectRoot}",
        "forwardAllArgs": true
      }
    }
  }
}
